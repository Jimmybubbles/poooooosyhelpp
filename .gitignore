# Data files (too large for git)
scanners/updated_Results_for_scan/
scanners/aussieMarketScan/asx_data/
scanners/ASXSCANNER/asx_data/

# Scan outputs (regenerated on every run — never commit)
scanners/buylist/
scanners/ultimate_scanner/
scanners/last_scan_results.json
scanners/fader_scan_results.json
scanners/efi_scan_results.json
scanners/last_run.log
scanners/last_refresh_date.txt
scanners/how_it_works_video.txt
scanners/etf_holdings/

# Python
__pycache__/
*.pyc
*.pyo

# OS
nul
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Database credentials - never commit this
website/db_config.py
db_config.py

# Runtime/server-side state — must be manually placed on PythonAnywhere,
# never committed. These mutate on every scan/update run, so if they were
# ever tracked, the next `git pull` would refuse (local changes would be
# overwritten) the same way the untracked-file conflict did for
# EFI_Indicator.py during the 2026-07-04 migration.
website/CSV/5000.csv
website/last_run.log
website/last_refresh_date.txt
website/last_*_results.json
website/skip_tickers.txt
website/deploy_log.txt
website/uploads/

# PythonAnywhere notes
scanners/pythonAnywhere.md
scanners/CSV/github.txt
